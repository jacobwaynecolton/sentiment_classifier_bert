# sentiment_classifier_bert
Fine-tuned `bert-base-uncased` on the SST2 dataset for binary sentiment classification, served via a FastAPI endpoint. Built independently without assistance as a practical exploration of the modern NLP fine-tuning workflow — specifically how pretrained transformer models are adapted for downstream tasks and deployed as a usable API.
